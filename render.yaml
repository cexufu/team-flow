services:
  - type: web
    name: teamflow-lite
    runtime: node
    region: singapore
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATA_DIR
        value: /var/data
      - key: INITIAL_ADMIN_PASSWORD
        sync: false
      - key: FEISHU_REMINDER_WEBHOOK
        sync: false
      - key: REMINDER_TIMEZONE
        value: Asia/Shanghai
      - key: REMINDER_HOUR
        value: 9
    disk:
      name: teamflow-data
      mountPath: /var/data
      sizeGB: 1
