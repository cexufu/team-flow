{
  "name": "teamflow-lite",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight internal team, task and product requirement management.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js",
    "test": "node tests/smoke.js"
  },
  "engines": {
    "node": ">=18"
  }
}
